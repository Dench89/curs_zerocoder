# curs_zerocoder

Изучение Python.

## Правило

Один проект = одна папка = одно виртуальное окружение (.venv).

This is my first commit
# Мой первый проект
git status
git add .
git commit -m "Описание"
git push
git log --oneline --graph --decorate --all